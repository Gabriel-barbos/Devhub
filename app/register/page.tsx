import { ProfileForm } from "@/components/register-page/profile-form"

export default function Page(){
    return (
        <div className="flex items-center justify-center h-screen">
            <ProfileForm />
        </div>
    )
}