import PrivateRoute from "@/components/auth/private-route"
import Sidebar from "@/components/shared/sidebar-component"

export default function LayoutNotifications({ children }: { children: React.ReactNode }) {
  return (
    <>
      {children}
    </>
  )
}